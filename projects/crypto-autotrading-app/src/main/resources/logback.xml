<configuration>
    <!-- ファイル出力用のAppender -->
    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>${APP_DATA_DIR:-data}/app.log</file>
        <append>true</append>
        <encoder>
            <charset>UTF-8</charset>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!--
         コンソールへのログ出力は行わず、ファイルのみに出力する。
         ※ ConsoleOutput.kt の println のみがコンソールに出るようになります
    -->
    <root level="debug">
        <appender-ref ref="FILE" />
    </root>
</configuration>
