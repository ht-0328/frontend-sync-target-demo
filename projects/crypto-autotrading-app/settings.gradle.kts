pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "crypto-autotrading-lab"

plugins {
    id("org.gradle.toolchains.foojay-resolver-convention") version "0.8.0"
}
